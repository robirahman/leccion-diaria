name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Copy web assets to dist
        run: |
          mkdir -p dist/icons
          cp *.html *.css *.js *.json manifest.json dist/
          cp icons/* dist/icons/ 2>/dev/null || true
          rm -f dist/bump-version.js dist/vocab_backup.js
          rm -f dist/package.json dist/package-lock.json 2>/dev/null || true
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist
      - id: deployment
        uses: actions/deploy-pages@v4
